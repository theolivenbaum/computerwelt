fail_fast: true

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: no-commit-to-branch # prevent direct commits to the `main` branch
      - id: check-yaml
      - id: check-toml
      - id: end-of-file-fixer
      - id: trailing-whitespace
      - id: check-added-large-files

  - repo: https://github.com/codespell-project/codespell
    # Configuration for codespell is in pyproject.toml
    rev: v2.3.0
    hooks:
      - id: codespell
        additional_dependencies:
          - tomli

  - repo: https://github.com/google/yamlfmt
    # Configuration for yamlfmt is in yamlfmt.yaml
    rev: v0.21.0
    hooks:
      - id: yamlfmt

  - repo: https://github.com/zizmorcore/zizmor-pre-commit
    # Configuration for yamlfmt is in .github/zizmor.yml
    rev: v1.23.1
    hooks:
      - id: zizmor

  - repo: local
    hooks:
      - id: format-rs
        name: Format Rust
        entry: make format-rs
        types: [rust]
        language: system
        pass_filenames: false
      - id: lint-rs
        name: Lint Rust
        entry: make lint-rs
        types: [rust]
        language: system
        pass_filenames: false
      - id: format--py
        name: Format Python
        entry: make format-py
        types: [python]
        language: system
        pass_filenames: false
      - id: lint-py
        name: Lint Python
        entry: make lint-py
        types: [python]
        language: system
        pass_filenames: false
      - id: format-js
        name: Format TypeScript
        entry: make format-js
        types: [ts]
        language: system
        pass_filenames: false
      - id: lint-js
        name: Lint TypeScript
        entry: make lint-js
        types: [ts]
        language: system
        pass_filenames: false
      # `custom/*.pyi` only reaches the type checker via the vendored copy
      - id: check-typeshed
        name: Check vendored typeshed
        entry: make check-typeshed
        files: ^crates/monty-typeshed/(custom|vendor)/
        language: system
        pass_filenames: false
