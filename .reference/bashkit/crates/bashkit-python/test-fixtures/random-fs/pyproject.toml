[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "bashkit-random-fs"
version = "0.1.0"
requires-python = ">=3.9"

[tool.maturin]
manifest-path = "Cargo.toml"
module-name = "bashkit_random_fs"
